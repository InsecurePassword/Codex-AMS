# AMS App-Task Lane

Optional separately installed AMS companion for a directly authorized user-visible Codex app task. Implicit invocation is disabled and core AMS has no persistent feature flag for it.
