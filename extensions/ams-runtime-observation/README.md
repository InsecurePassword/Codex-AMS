# AMS Runtime Observation

Optional separately installed AMS companion for directly requested bounded local runtime-metadata corroboration. Implicit invocation is disabled and core AMS has no persistent feature flag for it.
